# ANOMALIES.md — Отчёт об аномалиях в review.csv

## Общая информация

| Параметр | Значение |
|----------|----------|
| Файл | `review.csv` |
| Всего строк (без заголовка) | 207 |
| Уникальных ID | 203 |
| Пересечение ID с JSON-данными | 6 записей |
| Новых записей (только в CSV) | 197 |

---

## 1. Несоответствие названия и содержимого

**Файл называется `review.csv`**, но содержит не отзывы, а **профили компаний** — те же поля, что и в JSON-выгрузке (`id`, `name`, `category`, `city`, `address`, `rating`, `reviews_count`, `site`, `phone`). Ожидались бы поля вроде `review_text`, `author`, `date`, `review_rating`.

**Как обнаружено**: сравнил заголовки CSV с полями JSON — полное совпадение структуры.

---

## 2. Битая кодировка (Mojibake)

Два города содержат текст, перекодированный из UTF-8 через Windows-1251:

| Строка | Значение в CSV | Расшифровка |
|--------|---------------|-------------|
| 113 | `РњРѕСЃРєРІР°` | **Москва** |
| 155 | `РЎР°РЅРєС‚-РџРµС‚РµСЂР±СѓСЂРі` | **Санкт-Петербург** |

**Как обнаружено**: скрипт обнаружил кириллические символы `Р`, `С` в полях, характерные для двойного перекодирования UTF-8.

---

## 3. Аномалии в поле "город"

| Строка | ID | Значение `city` | Проблема |
|--------|----|-----------------|----------|
| 22 | c_001182 | `Санкат-Петербург` | Опечатка (к вместо т) |
| 37 | c_001015 | `ул. Советская, д. 89, офис 43` | Адрес попал в поле города (сдвиг колонок) |
| 127 | c_001108 | `Moscow` | Англоязычное написание |
| 148 | c_001184 | `Москва ` | Лишний пробел в конце |
| 166 | c_001047 | `москва` | Нижний регистр |
| 207-208 | (пусто) | (пусто) | Полностью пустые строки в конце файла |

**Как обнаружено**: подсчёт уникальных городов через `Counter` выявил нетипичные значения.

---

## 4. Аномалии в поле "рейтинг"

| Строка | ID | Значение | Проблема |
|--------|----|----------|----------|
| 14 | c_001010 | `4,5` | Запятая вместо точки (русская локаль) |
| 96 | c_001122 | `-3` | Отрицательный рейтинг (вне диапазона 0–5) |
| 197 | c_001186 | `7.2` | Рейтинг > 5.0 (вне допустимого диапазона) |
| Разные | — | `N/A` | Текстовая заглушка вместо пустого значения |

**Как обнаружено**: regex-проверка формата + проверка диапазона `[0, 5]`.

---

## 5. Аномалии в поле "число отзывов"

| Строка | ID | Значение | Проблема |
|--------|----|----------|----------|
| 61 | c_001116 | `-10` | Отрицательное число отзывов |
| 98 | c_001079 | `45.5` | Дробное число (должно быть целое) |
| 200 | c_001187 | `много` | Текстовое значение вместо числа |

**Как обнаружено**: попытка `int()` / `float()` конвертации + проверка `>= 0`.

---

## 6. Подозрительные ID

6 записей имеют ID из серии `c_900xxx`, которая не встречается в основной JSON-выгрузке (там `c_000001`–`c_000999`):

| ID | Компания |
|----|----------|
| c_900006 | — |
| c_900007 | — |
| c_900008 | АО Флагман Лаб |
| c_900009 | — |
| c_900010 | — |
| c_900011 | — |

Эти ID выглядят тестовыми или сгенерированными вне основного диапазона.

**Как обнаружено**: фильтрация ID, не совпадающих с паттерном `c_001xxx`.

---

## 7. Дубликаты ID внутри CSV

4 ID встречаются дважды в CSV-файле. Также 6 ID пересекаются с записями из JSON (совпадают полностью — дубликаты).

**Как обнаружено**: `Counter` по столбцу `id` + пересечение с множеством JSON ID.

---

## 8. Пустые строки

Строки 207 и 208 (последние в файле) — полностью пустые. Вероятно, артефакт экспорта.

---

## Итого

| Тип аномалии | Количество |
|--------------|-----------|
| Mojibake-кодировка | 2 |
| Опечатки/ошибки в городах | 5 |
| Сдвиг колонок | 1 |
| Невалидный рейтинг | 3+ |
| Невалидное число отзывов | 3 |
| Подозрительные ID (c_900xxx) | 6 |
| Пустые строки | 2 |
| Дубликаты ID | 4 |
| **Главное**: файл — не отзывы, а компании | ✓ |
